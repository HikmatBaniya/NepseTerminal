# ── Python ──────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# ── Virtual environment ──────────────────────────────────────────────────────
myvenv/
venv/
env/
.venv/

# ── IPC / runtime signal files (written at runtime, not source) ─────────────
killer.txt
trainer_status.json
trainer_last_training_time.txt

# ── Per-coin runtime data folders ───────────────────────────────────────────
# These are created automatically; keep folder structure but not generated files
NABIL/
NTC/
NICA/
NLIC/
SBL/
# Generic catch-all for any new coin folders added later
# (add specific coins above as needed)

# ── ML pattern memory files (large, generated by trainer) ───────────────────
memories_*.txt
memory_weights_*.txt
neural_perfect_threshold_*.txt

# ── Hub data / trade records ─────────────────────────────────────────────────
hub_data/

# ── NEPSE company list cache ─────────────────────────────────────────────────
nepse_companies.json

# ── GUI settings (user-local config) ────────────────────────────────────────
gui_settings.json

# ── API credentials (never commit) ──────────────────────────────────────────
r_key.txt
r_secret.txt
*.pem
*.key
secrets.json

# ── OS / editor junk ────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace

# ── Claude Code internal ─────────────────────────────────────────────────────
.claude/
